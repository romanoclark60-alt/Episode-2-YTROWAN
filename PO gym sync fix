I already have a dashboard at index.html using cross-device sync via a
Netlify Function at netlify/functions/store.mjs (talks to Netlify Blobs
over raw fetch, zero npm deps).

I now have a SECOND single-file HTML app in the same project that also
uses localStorage. I want it to sync across devices using the SAME
function, but stored under a separate blob so the two apps don't
overwrite each other.

Do this:

1. Open netlify/functions/store.mjs. Right now its blob URL ends with
   `/state` — a single hardcoded blob. Make it accept an optional
   `?key=<name>` query string and use that as the blob key. Default to
   `state` when missing so my existing dashboard sync keeps working
   unchanged. Validate the key with /^[A-Za-z0-9_-]{1,64}$/ and reject
   invalid keys with 400.

2. Read the second HTML app and figure out:
   - Every localStorage key it writes (look for `localStorage.setItem`
     and any constants like `const FOO_KEY = '...'`).
   - The render/refresh function(s) that need to be called after data
     changes (e.g. `renderAll()`, `wtRender()`).
   - Whether it keeps an in-memory copy of state (e.g.
     `let state = loadState()`) that must be reassigned after a remote
     pull, not just localStorage.

3. At the bottom of that app's main IIFE / script (after init renders
   run, before the IIFE closes), inject a cloud-sync block that:
   - Uses `/.netlify/functions/store?key=<short-app-slug>` (pick a slug
     like `po-coach`, `notes`, `recipes` — whatever fits the app).
   - Monkey-patches `localStorage.setItem` and `localStorage.removeItem`
     so any write to one of the synced keys triggers a debounced (~400ms)
     push of the current state.
   - On load, polls every 5s, on `focus`, and on `visibilitychange`
     (when visible), pulling remote state.
   - When applying remote: reassigns the in-memory state variable
     (using the app's own loadState-equivalent), then calls every render
     function so the UI redraws.
   - While the user is typing in an INPUT / TEXTAREA / SELECT /
     contenteditable, queue incoming remote state in a pendingRemote
     variable and apply on `focusout` — never wipe live input.
   - First-launch seed: if remote is empty AND local has data, push
     local up (so my existing local data isn't blown away by an empty
     remote on first sync).
   - Last-write-wins per key. No per-item merging.

4. Don't touch the dashboard's index.html — it already syncs and its
   default endpoint (no `?key=`) still works after the function update.

5. After you're done, remind me to:
   - Open the app on the device that already has the data FIRST after
     deploying, so it seeds the remote before any empty device pulls.
   - Hard-refresh every device.
   - Verify by visiting `/.netlify/functions/store?key=<slug>` directly
     — should show the JSON state, not an error.
