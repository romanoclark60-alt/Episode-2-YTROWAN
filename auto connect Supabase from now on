I just added a new HTML page to my dashboard project (alongside index.html
and the other tabs). I want it to sync across all my devices using the
SAME Supabase project that the rest of my dashboard already uses.

Do this:

1. Open one of my existing synced pages (e.g. index.html or po-coach.html)
   and find the two constants near the bottom of the <script> tag:
       const SUPABASE_URL = 'https://...supabase.co';
       const SUPABASE_KEY = 'sb_publishable_...';
   Copy those exact values — I do NOT want a different Supabase project,
   they need to match so everything lives in the same database.

2. Open the new HTML page I just added. Figure out:
   - Every localStorage key it writes (look for localStorage.setItem and
     any const NAME_KEY = '...' declarations).
   - Every in-memory variable that mirrors one of those keys (e.g.
     `let state = loadState()`, `let entries = load()`) that must be
     RELOADED from localStorage after a remote pull, otherwise the UI
     would render stale data after a sync.
   - Every render / refresh function that needs to be called after
     remote state is applied so the UI redraws.

3. At the bottom of that page's main IIFE / <script>, BEFORE the closing
   `})();`, paste a Supabase sync block that:
   - Loads the Supabase JS client via `<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>`
     in the <head> if not already there.
   - Picks a unique short slug for `const APP_KEY = '...'` (use the
     filename minus `.html`, e.g. 'notes', 'meals', 'budget' — must NOT
     collide with 'dashboard', 'po-coach', or 'health').
   - Stores the page's full synced state as one JSONB row in the
     `public.app_state` table, keyed by APP_KEY.
   - Monkey-patches `localStorage.setItem` / `localStorage.removeItem`
     so any write to one of the synced keys schedules a debounced
     (~250ms) push to Supabase. The original set/remove always runs
     first and the sync code is wrapped in try/catch so a sync bug can
     never block a local save.
   - On initial load: pulls the current row from Supabase, applies it,
     then subscribes to `postgres_changes` events for that row. Any
     change pushed by another device is applied within ~1 second.
   - Tracks `lastSyncedJson` so realtime events that echo our own pushes
     are ignored (no infinite push loop).
   - While the user is typing in an INPUT / TEXTAREA / SELECT /
     contenteditable, queues incoming remote state in a `pendingRemote`
     variable and applies it on `focusout` — never wipes live input.
   - On `pagehide`, `beforeunload`, and `visibilitychange:hidden`,
     fires a fetch with `keepalive: true` to flush any pending change
     even if the tab is closing. (Don't use sendBeacon — Supabase needs
     auth headers it doesn't support.)
   - When applying remote state, reassigns the in-memory variables
     (using the page's own load helpers) AND calls every render function
     so the UI actually updates.
   - First-launch seed: if remote is empty but local has data, push the
     local data up so my existing work isn't blown away by an empty
     remote on first sync.

4. Don't touch the OTHER pages' sync blocks. They already work.

5. After you're done, remind me to:
   - Hard-refresh every device once.
   - Verify by tweaking something in the new page on one device and
     watching it appear on another within ~1 second.
   - Confirm the SQL block from CLAUDE.md has been run in Supabase
     (so the app_state table exists with the right RLS policies).

Deploy when you're done.
